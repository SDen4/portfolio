body {
    font-family: Open Sans;
}

ul {
    margin: 0;
    padding: 0;
}

li {
    list-style-type: none;
}

.main-title {
    display: flex;
    align-items: flex-start;
}

.pre-title {
    color: $text-color-gray;
    font-size: 18px;
    font-weight: 600;
    text-align: right;
    max-width: 275px;
}

.title_owner {
    font-size: 72px;
    font-weight: 800;
    text-align: center;
    max-width: 400px;
}